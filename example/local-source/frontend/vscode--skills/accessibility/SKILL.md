# Accessibility Skill

Guidelines for building accessible UI components.

## Rules
- Use semantic HTML elements
- Ensure sufficient color contrast (WCAG AA)
- Add ARIA labels to interactive elements
- Support keyboard navigation
