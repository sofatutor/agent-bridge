# Testing Skill

Guidelines for writing high-quality tests.

## Guidelines

- Use Vitest as the test runner
- Prefer `describe` / `it` blocks
- Mock external dependencies, not internal modules
- Always test edge cases and error paths
